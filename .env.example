# Copy this file to `.env.local` and add your free API key.
#
# Option 1 (recommended for local dev): Google Gemini — free tier with vision support
# Get a key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Option 2: Lovable Cloud (only if deploying on Lovable)
# LOVABLE_API_KEY=
